import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        paper: "#faf9f6",
        ink: "#262626",
        muted: "#66645f",
        line: "#dfdbd2",
        accent: "#745e47",
        "accent-soft": "#eee9e1",
      },
    },
  },
};

export default config;
